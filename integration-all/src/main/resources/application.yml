server:
  port: 9999
  servlet:
    context-path: /all/api
spring:
  profiles:
    active: dev
custom:
  log:
    pattern: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{90} - %msg%n"
    dir: app-demo

logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG

